{% extends 'layout.html.twig' %}

{% block title %}New Project{% endblock %}

{% block main %}
        {{ form_start(form) }}
            <div class="form-horizontal">
                <section>
                    <h3>New Project</h3>
                    <div class="row mt-4">
                        <div class="offset-md-4 col-md-8">
                            <div class="form-group row">
                                {{ form_errors(form.name) }}
                                {{ form_label(form.name) }}
                                {{ form_widget(form.name, { 'attr': {'class': 'form-control'} }) }}
                            </div>
                            <div class="form-group row">
                                {{ form_errors(form.description) }}
                                {{ form_label(form.description) }}
                                {{ form_widget(form.description, { 'attr': {'class': 'form-control'} }) }}
                            </div>
                            <div class="form-group row">
                                {{ form_errors(form.gitlink) }}
                                {{ form_label(form.gitlink) }}
                                {{ form_widget(form.gitlink, { 'attr': {'class': 'form-control'} }) }}
                            </div>
                            <div class="form-group row">
                                {{ form_errors(form.startDate) }}
                                {{ form_label(form.startDate) }}
                                {{ form_widget(form.startDate, { 'attr': {'class': 'form-control'} }) }}
                            </div>
                            <div class="form-group row">
                                {{ form_errors(form.endDate) }}
                                {{ form_label(form.endDate) }}
                                {{ form_widget(form.endDate, { 'attr': {'class': 'form-control'} }) }}
                            </div>
                            <div class="form-group row">
                                {{ form_label(form.uploadedFiles) }}
                                {{ form_errors(form.uploadedFiles) }}
                                {{ form_widget(form.uploadedFiles, { 'attr': {'class': 'form-control'} }) }}
                            </div>
                        </div>
                    </div>
                </section>
                <hr/>
                <div class="row">
                    <div class="offset-md-6 col-md-6">
                        <button type="submit" class="btn btn-success"><i class="fa fa-save"></i>Add</a>
                    </div>
                </div>
            </div>
        {{ form_end(form) }}
{% endblock %}